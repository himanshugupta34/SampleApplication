package com.example.testapplication.base

import android.app.Application
import androidx.lifecycle.AndroidViewModel

abstract class BaseViewModel(mApplication: Application) : AndroidViewModel(mApplication)